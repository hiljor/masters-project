[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "enclose.horse algorithms"
version = "0.1.0"
dependencies = [] # Add your requirements here (e.g., "requests")

[tool.setuptools.packages.find]
where = ["src"]